const Sticker = ({
  text,
  title,
  className,
}: {
  text: string;
  title: string;
  className: string;
}) => {
  return (
    <div className={className}>
   
      
    </div>
  );
};

export default Sticker;
